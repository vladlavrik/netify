export enum BreakpointStage {
	Request = 'Request',
	Response = 'Response',
	Both = 'Both',
}

export const breakpointStagesList = [BreakpointStage.Request, BreakpointStage.Response, BreakpointStage.Both];
